# -*- coding: utf-8 -*-
"""
Банк стереометрии пересоздан с нуля, задачи больше не копии банка «Решу ЕГЭ».
Страницы, которые утверждали обратное, обязаны говорить правду.
Три файла, все правки с assert-якорями.

Примечание 24.09.2026: утверждение «оригинальный data.js утрачен» оказалось
неверным — старый банк (143 задачи открытого банка, числовые id) лежал
в опубликованной линейке trainers/ege-profile-stereometry-3d (коммит 4dc0592)
и возвращён в объединённый банк (problems-legacy-*.js). Тексты, которые
этот скрипт вписал в страницы, заменены честными — см. _istochniki/README.md.
"""
import io
import os

# Путь сборочной машины архива убран 24.09.2026; скрипт уже применён.
# Корень дерева курса задаётся только явно — переменной COURSE_ROOT.
if not os.environ.get("COURSE_ROOT"):
    raise SystemExit("история: скрипт уже применён 22.09.2026; корень курса — только через COURSE_ROOT")
ST = os.path.join(os.environ["COURSE_ROOT"], "trainers", "stereo")


def rep(path, pairs):
    t = io.open(path, encoding="utf-8").read()
    for old, new, cnt in pairs:
        n = t.count(old)
        assert n == cnt, "%s: якорь x%d (ждали %d): %r" % (path, n, cnt, old[:70])
        t = t.replace(old, new)
    io.open(path, "w", encoding="utf-8").write(t)
    print("%s: %d правок" % (os.path.basename(path), len(pairs)))


# 1. хаб
rep(os.path.join(ST, "index.html"), [
    ("""    <p class="hub-sub">Интерактивные 3D-тренажёры по всем темам задания 3 профильного ЕГЭ по математике.
    Каждая задача — из банка Решу ЕГЭ, с вращающимся чертежом, кликабельными линиями,
    анимацией построений, подсказками и решениями.</p>""",
     """    <p class="hub-sub">Интерактивные 3D-тренажёры по всем темам задания 3 профильного ЕГЭ по математике.
    Задачи составлены заново по типовым моделям первой части экзамена; у каждой —
    вращающийся чертёж, кликабельные линии, анимация построений, подсказка и решение.</p>""", 1),
])

# 2. страница тренажёра
rep(os.path.join(ST, "trainer.html"), [
    ('<div class="subtitle">ЕГЭ профиль · банк Решу ЕГЭ · тема: <b id="topicName"></b> · <a href="index.html">все тренажёры</a></div>',
     '<div class="subtitle">ЕГЭ профиль · типовые модели задания 3 · тема: <b id="topicName"></b> · <a href="index.html">все тренажёры</a></div>', 1),
])

# 3. README пакета
rep(os.path.join(ST, "README.txt"), [
    ("""143 задачи банка Решу ЕГЭ по 9 темам с интерактивными 3D-чертежами:""",
     """143 задачи по 9 темам с интерактивными 3D-чертежами. Банк пересоздан
заново 22.09.2026 (оригинальный js/data.js был утрачен): формулировки и
числовые параметры составлены по типовым моделям первой части ЕГЭ, а не
скопированы из чужих банков; каждый ответ пересчитан независимым скриптом
от текста условия, каждая сцена прогнана в Chromium и просмотрена глазами.
Верификаторы и исходники банка — в архиве выкладки, папка _istochniki.

Распределение по темам:""", 1),
    ("""  • Проверка ответа (запятая и точка равнозначны), подсказка, полное
    решение, ссылка на первоисточник Решу ЕГЭ.""",
     """  • Проверка ответа (запятая и точка равнозначны), подсказка, полное
    решение; если у задачи задано поле src, показывается ссылка
    на прототип в банке Решу ЕГЭ.""", 1),
    ("""КАК ДОБАВЛЯТЬ СВОИ ЗАДАЧИ
  Откройте js/data.js и допишите объект в массив PROBLEMS по образцу
  соседних задач той же темы (поля: id, topic, cond, ans, scene/dims,
  labels, construct, hint, sol). Новая задача автоматически появится
  в навигации и на главной странице.""",
     """КАК ДОБАВЛЯТЬ СВОИ ЗАДАЧИ
  Откройте js/data.js и допишите объект в тематический массив (P_KUB,
  P_PAR, …) по образцу соседних задач (поля: id, topic, group, cond, ans,
  scene с телами, labels, givenFaces, construct, hint, sol). Сцены
  собираются строителями box / lshape / prism / pyramid / cyl / cone /
  sphere — их параметры описаны в комментариях в начале файла. Новая
  задача автоматически появится в навигации и на главной странице.""", 1),
])
