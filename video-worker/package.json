{
  "name": "mathexam-video-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated queue, speech and MP4 renderer for Mathexam video scenes.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test --test-concurrency=1 test/*.test.js"
  },
  "dependencies": {
    "playwright": "1.55.0"
  }
}

