# История правок — НЕ ЗАПУСКАТЬ

Все скрипты в этой папке уже применены к дереву курса (в архиве —
`profile-ege-course/`, в репозитории — `ege-profil/`) 22.09.2026. Лежат для истории: по ним видно, что именно и зачем менялось.

Повторный запуск ничего не сломает (правки защищены assert-якорями
и упадут на уже изменённых файлах), но и смысла не имеет. Пути сборочной
машины из скриптов убраны 24.09.2026: без явной переменной COURSE_ROOT
скрипт сразу останавливается.

| скрипт | что сделал |
|---|---|
| `apply-sept-patch.py` | вернул сентябрьскую интеграцию тренажёра треугольника в навигатор и журнал |
| `splice-bank18.py` | вернул в `exam/bank.js` расширенный модуль линии 18 |
| `make-graphic.py` | собрал `parameters-18/graphic.html` по рецепту из чата |
| `add-back-links.py` | добавил плашку «← Курс» на четыре страницы-тупика |
| `patch-navigator.py` | мини-курс параметров в маршрут, ссылки рационализации |
| `dorabotka5.py` | пять доработок после прохода «глазами ученика» |
| `patch-stereo-trainer.txt` | две точечные правки движка стереометрии |

Запускаемые ворота — уровнем выше: `patches/check-links.py`,
`patches/verify5.js`, и в `_istochniki/stereo-bank/` — `sync-data.js`,
`verify-*.js` и `legacy-parity.js`. Как запускать — `_istochniki/README.md`.
